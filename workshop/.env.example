# Secretos de tu Slack App. Copiá este archivo a `.env` y completá los valores.
#
#   cp .env.example .env
#   # editá .env con tus datos, y antes de correr los pasos:
#   set -a; source .env; set +a
#
# Dónde sacar cada uno (en api.slack.com/apps → tu app):
SLACK_SIGNING_SECRET=    # Basic Information → App Credentials → Signing Secret
SLACK_BOT_TOKEN=         # OAuth & Permissions → Bot User OAuth Token (empieza con xoxb-)
SLACK_BOT_USER_ID=       # lo da auth.test; o el User ID del bot (empieza con U)

# Jira (opcional) — si los completás, el agente gana las tools buscar_jira / crear_jira.
# El API token se crea en id.atlassian.com/manage-profile/security/api-tokens
JIRA_BASE_URL=           # ej. https://tuempresa.atlassian.net
JIRA_EMAIL=              # el email de tu cuenta de Atlassian
JIRA_API_TOKEN=          # API token (NO tu contraseña)
JIRA_PROJECT=            # project key donde crear tickets (ej. PROJ)
